// Package providers wires optional providers into the binary via build tags.
// Each file in this package carries a build tag; only the files whose tags
// are active at compile time are included.
package providers
